# AI policy — website

This subproject follows the repository-root [`AI_POLICY.md`](../AI_POLICY.md).

Files carrying a `[!IMPORTANT] Human review needed` marker (in a comment or an
`<!-- -->` block) are AI-generated and awaiting human review. Remove the marker
once a human has reviewed the file. The root `just review` recipe counts
outstanding markers across the repo.
